import Link from "next/link";
export const metadata={title:"Installation | ByGeorge BIM"};
export default function Install(){return <main className="bim-document bim-narrow"><p className="bim-eyebrow">GET SET UP</p><h1>From ZIP<br/>to Revit ribbon.</h1><h2>Before you start</h2><ul><li>Windows and a licensed installation of full Autodesk Revit. Revit LT cannot run these tools.</li><li>Install a pyRevit version compatible with your Revit version, using its IronPython engine. See the <a href="https://github.com/pyrevitlabs/pyRevit/releases" target="_blank" rel="noreferrer">official pyRevit releases</a>.</li><li>The suite targets Revit 2024–2026. Some individual sources target 2023. Runtime verification is pending; check your product’s compatibility notes.</li><li>An internet connection for activation and each tool launch.</li><li>Close every Revit window before installing. Test model-changing tools on a copy of your project.</li></ul><h2>Install your download</h2><ol><li>Save your key from the purchase receipt. Download the ZIP and use Windows “Extract All.”</li><li>Open the extracted folder. Read INSTALL.txt, then right-click Install-ByGeorge.ps1 and choose “Run with PowerShell.” The installer does not require administrator access or change your execution policy.</li><li>If Windows blocks PowerShell scripts, use the manual steps below. Do not disable your organization’s security policy.</li><li>Open Revit. Find the ByGeorge tab. If needed, use pyRevit → Reload.</li><li>Click a purchased tool. Enter your activation key when prompted. The same key works on up to two computers.</li></ol><h2>Manual installation</h2><p>Copy the included <code>ByGeorge.extension</code> folder into <code>%APPDATA%\pyRevit\Extensions</code>. If a ByGeorge.extension folder already exists, rename it to a dated backup outside the Extensions folder first. Do not merge the folders.</p><h2>Already using ByGeorge?</h2><p>The installer stops if Revit is open. It moves an existing extension into a dated backup folder outside pyRevit’s extension scan path before copying the new one. To roll back, close Revit, move the new extension aside, and restore your backup.</p><h2>Activation help</h2><p>Keys are saved for your Windows user using Windows encryption. Keys only unlock purchased features. Moving to a third computer requires a support reset. A failed online check shows a clear message and leaves the model unchanged.</p><Link href="/bim/terms">License, refunds, and support ↗</Link></main>;}
